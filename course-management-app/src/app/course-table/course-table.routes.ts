import { Routes } from '@angular/router';
import { CourseTableComponent } from './course-table.component';

export const COURSETABLE_ROUTES: Routes = [
  { path: '', component: CourseTableComponent },
];
